<% @user ||= current_user %>
<div class="stats">
  <p class="pt-2 pl-2"><a href="<%= following_user_path(@user) %>">
    <strong id="following" class="stat">
      <%= @user.followings.count %>
    </strong>
    フォロー 
  </a></p>
  <p class="pl-2"><a href="<%= followers_user_path(@user) %>">
    <strong id="followers" class="stat">
      <%= @user.followers.count %>
    </strong>
    フォロワー
  </a></p>
</div>
