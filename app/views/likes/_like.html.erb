<% if current_user.likes.find_by(post_id: post.id) %>
  <%= link_to unlike_path(post.id), method: :delete do %>
    <div class="like">♥</div>
  <% end %>
<% else %>
  <%= link_to like_path(post.id), method: :post do %>
    <div class="unlike">♡️</div>
  <% end %>
<% end %>
<%= post.likes.count %>
